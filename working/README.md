# working/ — working notes (not canonical)

Reference / in-progress notes moved here from the local `~/hami-cluster-test` working dir during
the pre-redeploy consolidation (cluster 230 is being destroyed and rebuilt with Warewulf + the
modular RKE2 manifests in `deploy-aleph/rke2-manifests/`).

These are **working notes, not canonical docs** — they carry cluster-specific references (230/232
IPs, local paths) and reflect state at the time of writing. Canonical docs live in `docs/` and
the per-model dirs under `models/`.

- `MODEL-CAMPAIGN-PLAN.md` — the per-model test + artifact-authoring campaign plan (chat LLMs + embeddings).
- `LLM-MODEL-TRACKER.md` — campaign tracker notes, research, gotchas.
- `CLUSTER-230-PLAN.md` — how the (old) 230 cluster was set up; reference for the redeploy.
- `MIGRATION-232-to-230.md` — 232↔230 model/repo mapping from the migration.
