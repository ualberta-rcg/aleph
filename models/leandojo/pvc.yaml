apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: leandojo-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 10Gi
