apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: boltz-2
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 30Gi
