apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astroclip-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
