apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geogalactica-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 80Gi
  storageClassName: nfs-client
