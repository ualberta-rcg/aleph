apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: time-moe
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 5Gi
