apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uma-m-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-client
  resources:
    requests:
      storage: 30Gi
