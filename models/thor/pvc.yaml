apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thor-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 4Gi
  storageClassName: nfs-client
