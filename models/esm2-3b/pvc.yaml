apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esm2-3b-data
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-models
  resources:
    requests:
      # ESM-2 3B weights ~12Gi; subPath `pysite` holds pip packages
      storage: 30Gi
