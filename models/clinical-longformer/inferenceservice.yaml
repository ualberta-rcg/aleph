# Clinical-Longformer: Longformer fine-tuned on clinical notes (yikuan8/Clinical-Longformer)
# 149M params, Apache 2.0. 496K downloads. Handles long clinical documents up to 4096 tokens.
# Pre-trained on MIMIC-III clinical notes. Superior to ClinicalBERT for long clinical texts.
# API: POST /v1/science/embed  (clinical text → embedding, supports long documents)
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: clinical-longformer-server
  namespace: models
data:
  server.py: |
    """
    Clinical-Longformer: Long clinical document embeddings (up to 4096 tokens).
    API: POST /v1/science/embed -> {"text": "Patient presents with..."} -> {"embedding": [...]}
    """
    import os, asyncio
    from fastapi import FastAPI
    from fastapi.responses import JSONResponse
    import uvicorn

    MODEL_DIR = os.environ.get("MODEL_DIR", "/data/model")
    app = FastAPI()
    tokenizer = None
    model = None
    DEVICE = "cpu"

    def load():
        global tokenizer, model, DEVICE
        import torch
        from transformers import AutoTokenizer, AutoModel
        DEVICE = "cuda" if torch.cuda.is_available() else "cpu"
        print(f"Loading Clinical-Longformer on {DEVICE}...", flush=True)
        tokenizer = AutoTokenizer.from_pretrained(MODEL_DIR)
        model = AutoModel.from_pretrained(MODEL_DIR).to(DEVICE)
        model.eval()
        print(f"Clinical-Longformer ready on {DEVICE}", flush=True)

    @app.on_event("startup")
    async def startup():
        await asyncio.get_event_loop().run_in_executor(None, load)

    @app.get("/health")
    async def health():
        return {"status": "ok" if model is not None else "loading", "model": "clinical-longformer"}

    @app.get("/v1/models")
    async def list_models():
        return {"models": [{"id": "clinical-longformer", "description": "Clinical Longformer (4096 token context, 496K downloads)"}]}

    @app.post("/v1/science/embed")
    async def embed(payload: dict):
        import torch
        if model is None:
            return JSONResponse({"error": "model loading"}, 503)
        texts = payload.get("texts") or ([payload.get("text")] if payload.get("text") else None)
        if not texts:
            return JSONResponse({"error": "provide text or texts"}, 400)
        enc = tokenizer(texts, return_tensors="pt", padding=True, truncation=True, max_length=4096)
        enc = {k: v.to(DEVICE) for k, v in enc.items()}
        # Longformer needs global attention on CLS token
        if "global_attention_mask" not in enc:
            enc["global_attention_mask"] = torch.zeros_like(enc["input_ids"])
            enc["global_attention_mask"][:, 0] = 1
        with torch.no_grad():
            out = model(**enc)
        embeddings = out.last_hidden_state[:, 0, :].cpu().tolist()
        return {"embeddings": embeddings, "model": "clinical-longformer", "dim": len(embeddings[0])}

    @app.post("/v1/embeddings")
    async def openai_embeddings(payload: dict):
        # OpenAI-style: {model, input(str|list)} -> {object, data:[{embedding}], model, usage}
        import torch
        if model is None:
            return JSONResponse({"error": "model loading"}, 503)
        inp = payload.get("input")
        if inp is None:
            inp = payload.get("texts") or ([payload.get("text")] if payload.get("text") else None)
        texts = [inp] if isinstance(inp, str) else list(inp or [])
        if not texts or not texts[0]:
            return JSONResponse({"error": "provide 'input' (string or list)"}, 400)
        enc = tokenizer(texts, return_tensors="pt", padding=True, truncation=True, max_length=4096)
        enc = {k: v.to(DEVICE) for k, v in enc.items()}
        # Longformer needs global attention on the CLS token (matches /v1/science/embed)
        if "global_attention_mask" not in enc:
            enc["global_attention_mask"] = torch.zeros_like(enc["input_ids"])
            enc["global_attention_mask"][:, 0] = 1
        with torch.no_grad():
            out = model(**enc)
        emb = out.last_hidden_state[:, 0, :].cpu().tolist()   # [CLS] pooling
        data = [{"object": "embedding", "index": i, "embedding": e} for i, e in enumerate(emb)]
        pt = sum(len(t.split()) for t in texts)
        return {"object": "list", "data": data, "model": payload.get("model", "clinical-longformer"),
                "usage": {"prompt_tokens": pt, "total_tokens": pt}}

    if __name__ == "__main__":
        uvicorn.run(app, host="0.0.0.0", port=8080)
---
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: clinical-longformer
  namespace: models
  annotations:
    autoscaling.knative.dev/scale-down-delay: "5m"
    autoscaling.knative.dev/scale-to-zero-pod-retention-period: "15m"
spec:
  predictor:
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: node-role.kubernetes.io/control-plane
                  operator: DoesNotExist
    minReplicas: 0
    maxReplicas: 2
    scaleTarget: 4
    scaleMetric: concurrency
    nodeSelector:
      gpu: "on"
    volumes:
      - name: model-data
        persistentVolumeClaim:
          claimName: clinical-longformer-data
      - name: server-code
        configMap:
          name: clinical-longformer-server
    initContainers:
      - name: model-setup
        image: python:3.11-slim
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
          limits:
            memory: "4Gi"
            cpu: "2"
        volumeMounts:
          - name: model-data
            mountPath: /data
        env:
          - name: HF_TOKEN
            valueFrom:
              secretKeyRef:
                name: hf-token
                key: token
          - name: HF_HOME
            value: /data/hf_cache
        command: ["bash", "-c"]
        args:
          - |
            set -e
            VENV=/data/venv
            if [ ! -f "$VENV/bin/python" ] || ! $VENV/bin/python -c "import transformers, fastapi" 2>/dev/null; then
              python -m venv $VENV --clear
              $VENV/bin/pip install --quiet --upgrade pip
              $VENV/bin/pip install --quiet torch --index-url https://download.pytorch.org/whl/cu126
              $VENV/bin/pip install --quiet transformers huggingface_hub fastapi uvicorn
              echo "Venv ready"
            else
              echo "Venv OK"
            fi
            if [ ! -f /data/model/config.json ]; then
              $VENV/bin/python -c "
            from huggingface_hub import snapshot_download; import os
            snapshot_download(repo_id='yikuan8/Clinical-Longformer', local_dir='/data/model', token=os.environ['HF_TOKEN'], ignore_patterns=['*.msgpack','flax_model*','tf_model*'])
            print('Done')
            "
            fi
    containers:
      - name: kserve-container
        image: python:3.11-slim
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
            nvidia.com/gpu: "1"
            nvidia.com/gpumem: "10240"
          limits:
            memory: "4Gi"
            cpu: "2"
            nvidia.com/gpu: "1"
            nvidia.com/gpumem: "10240"
        volumeMounts:
          - name: model-data
            mountPath: /data
          - name: server-code
            mountPath: /app
        env:
          - name: MODEL_DIR
            value: /data/model
          - name: HF_HOME
            value: /data/hf_cache
          - name: HF_HUB_OFFLINE
            value: "1"
        command: ["bash", "-c"]
        args:
          - |
            /data/venv/bin/python /app/server.py
        ports:
          - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 15
          failureThreshold: 10
