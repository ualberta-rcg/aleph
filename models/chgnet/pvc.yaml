apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chgnet-data
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-models
  resources:
    requests:
      # Weights small; venv ~2Gi
      storage: 10Gi
