apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bge-reranker-v2-m3-data
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
