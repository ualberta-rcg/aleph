apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mattergen-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-client
  resources:
    requests:
      storage: 15Gi
