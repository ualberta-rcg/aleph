apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astrosage
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 25Gi
