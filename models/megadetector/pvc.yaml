apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: megadetector-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 10Gi
