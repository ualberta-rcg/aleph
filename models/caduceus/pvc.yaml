apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: caduceus-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-models
