apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oceangpt-30b
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-models
  resources:
    requests:
      # 61 GB weights + headroom
      storage: 80Gi
