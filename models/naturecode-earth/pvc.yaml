apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: naturecode-earth-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 2Gi
  storageClassName: nfs-models
