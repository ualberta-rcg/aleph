apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: diffdock-nim
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 100Gi
