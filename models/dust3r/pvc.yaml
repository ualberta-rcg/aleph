apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dust3r-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 10Gi
