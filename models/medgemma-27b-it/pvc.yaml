apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: medgemma-27b-it
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 60Gi
