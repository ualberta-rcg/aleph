apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seisbench-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-client
  resources:
    requests:
      storage: 10Gi
