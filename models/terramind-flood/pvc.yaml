apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terramind-flood-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 4Gi
  storageClassName: nfs-models
