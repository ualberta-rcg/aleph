apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prithvi-wxc-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 60Gi
  storageClassName: nfs-client
