# Weights persist on NFS (nfs-models default SC = OneFS-safe 128Ki wsize) so scale-from-zero
# cold starts skip the ~16GB re-download. See storage/nfs-models-storageclass.yaml.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen25-vl-7b-data
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-models
  resources:
    requests:
      storage: 40Gi
