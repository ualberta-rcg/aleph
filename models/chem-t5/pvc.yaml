apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chem-t5
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 10Gi
