apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rfdiffusion
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-models
  resources:
    requests:
      storage: 50Gi
