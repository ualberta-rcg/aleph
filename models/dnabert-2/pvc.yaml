apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dnabert-2-data
  namespace: models
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
