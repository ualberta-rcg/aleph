apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ithaca
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 4Gi
  storageClassName: nfs-models
