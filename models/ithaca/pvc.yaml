apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ithaca-data
  namespace: models
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 4Gi
  storageClassName: nfs-client
